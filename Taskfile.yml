tasks:

  build:
    cmds:
      - fish -c 'for boat in boats/*; echo Fishing $boat …; fisher $boat; end'
      - fish -c 'fisher up'

  rebuild:
    cmds:
      - git clean -xd -f
      - task: build
      - git checkout .
  
  test:
    cmds:
      - fish -c 'fishtape **.test.fish'

output: 'prefixed'
version: '2'

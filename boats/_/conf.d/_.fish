# It's important that this declaration lives in the first boat (in glob order).
# Otherwise, after installing all boats, there is a change in fishd.*,
# as fish_user_abbreviations have become universal.
set -g fish_user_abbreviations
